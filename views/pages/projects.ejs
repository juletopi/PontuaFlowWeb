<div class="container-lg">
  <div class="row align-items-center mb-4">
    <div class="col-auto">
      <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#projectModal">
        <i class="bi bi-plus-circle"></i> Novo Projeto
      </button>
    </div>
  </div>

  <% if (projects && projects.length > 0) { %>
    <div class="row g-4">
      <% projects.forEach(project => { %>
        <div class="col-md-6 col-lg-4">
          <a class="project-link" href="/project/<%= project.id %>" aria-label="Acessar projeto <%= project.name %>">
            <div class="card project-card h-100 cursor-pointer">
              <div class="card-img-top project-image" style="background-color: var(--primary-color);">
                <% if (project.photo) { %>
                  <img src="/images/<%= project.photo %>" alt="<%= project.name %>">
                <% } else { %>
                  <div class="d-flex align-items-center justify-content-center h-100">
                    <i class="bi bi-folder-fill text-white" style="font-size: 3rem;"></i>
                  </div>
                <% } %>
              </div>
              <div class="card-body">
                <h5 class="card-title"><%= project.name %></h5>
                <p class="card-text text-muted"><%= project.description || 'Sem descrição' %></p>
              </div>
              <div class="card-footer bg-transparent border-top">
                <small class="text-muted">
                  <i class="bi bi-calendar-event"></i> Criado em <%= new Date(project.createdAt).toLocaleDateString('pt-BR') %>
                </small>
              </div>
            </div>
          </a>
        </div>
      <% }) %>
    </div>
  <% } else { %>
    <div class="alert alert-info" role="alert">
      <i class="bi bi-info-circle"></i> Nenhum projeto cadastrado ainda. <a href="#" data-bs-toggle="modal" data-bs-target="#projectModal">Crie o seu primeiro projeto!</a>
    </div>
  <% } %>
</div>

<!-- Modal novo projeto -->
<div class="modal fade" id="projectModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Novo Projeto</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <form id="projectForm">
        <div class="modal-body">
          <div class="mb-3">
            <label for="projectName" class="form-label">Nome do Projeto</label>
            <input type="text" class="form-control" id="projectName" required>
          </div>
          <div class="mb-3">
            <label for="projectDescription" class="form-label">Descrição (opcional)</label>
            <textarea class="form-control" id="projectDescription" rows="3"></textarea>
          </div>
          <div class="mb-3">
            <label for="projectPhoto" class="form-label">Foto/Avatar (opcional)</label>
            <input type="file" class="form-control" id="projectPhoto" accept="image/*">
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
          <button type="submit" class="btn btn-primary">Criar Projeto</button>
        </div>
      </form>
    </div>
  </div>
</div>

<script>
  $(function() {
    const apiBase = (window.API_URL || '').replace(/\/$/, '');

    $('#projectForm').on('submit', async function(e) {
      e.preventDefault();

      if (!apiBase) {
        alert('API_URL não configurada no servidor.');
        return;
      }

      const payload = {
        name: $('#projectName').val().trim(),
        description: $('#projectDescription').val().trim(),
      };

      const photoInput = $('#projectPhoto')[0];

      if (photoInput && photoInput.files && photoInput.files.length > 0) {
        payload.photo = photoInput.files[0].name;
      }

      try {
        const response = await fetch(`${apiBase}/projects`, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify(payload),
        });

        if (!response.ok) {
          throw new Error('Falha ao criar projeto');
        }

        window.location.reload();
      } catch (error) {
        alert('Não foi possível criar o projeto. Verifique a API e tente novamente.');
      }
    });
  });
</script>

<style>
  .project-link {
    display: block;
    color: inherit;
    text-decoration: none;
  }

  .project-link:hover .project-card {
    transform: translateY(-3px);
    box-shadow: 0 12px 28px rgba(15, 23, 42, 0.12);
  }

  .project-card {
    transition: transform 0.18s ease, box-shadow 0.18s ease;
  }

  .cursor-pointer {
    cursor: pointer;
  }
</style>
